import React from 'react';
import { ScrollView, Text } from 'react-native';

const FailedRecordTab = (): JSX.Element => {
  return (
    <ScrollView>
      <Text>실패이력</Text>
    </ScrollView>
  );
};

export default FailedRecordTab;
