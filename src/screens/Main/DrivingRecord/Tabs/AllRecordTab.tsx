import React from 'react';
import { ScrollView, Text } from 'react-native';

const AllRecordTab = (): JSX.Element => {
  return (
    <ScrollView>
      <Text>전체이력</Text>
    </ScrollView>
  );
};

export default AllRecordTab;
